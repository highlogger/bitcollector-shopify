{% comment %}
  Renders the 404 page content.
{% endcomment %}

<section class="max-w-md mx-auto px-4 py-20 sm:py-32 text-center space-y-6">
  <div class="inline-flex w-16 h-16 rounded-2xl bg-white/[0.02] border border-white/[0.06] items-center justify-center text-electric-blue text-2xl font-black">
    404
  </div>
  
  <h1 class="text-3xl font-bold text-white tracking-tight">
    Aradığınız Sayfa Bulunamadı
  </h1>
  
  <p class="text-sm text-silver leading-relaxed">
    İstediğiniz sayfa taşınmış, silinmiş veya geçici olarak kullanım dışı kalmış olabilir.
  </p>

  <div class="pt-4">
    <a href="{{ routes.root_url }}" class="btn-primary inline-flex items-center gap-2">
      <span>Ana Sayfaya Dön</span>
      <svg class="w-4 h-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 19-7-7 7-7"/><path d="M19 12H5"/></svg>
    </a>
  </div>
</section>

{% schema %}
{
  "name": "404 Sayfa İçeriği",
  "settings": []
}
{% endschema %}
