{% comment %}
  Renders the standard static page content.
{% endcomment %}

<section class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-20">
  <h1 class="text-3xl sm:text-5xl font-extrabold text-white mb-8 border-b border-white/[0.04] pb-6">
    {{ page.title }}
  </h1>
  <div class="prose prose-invert max-w-none text-silver text-sm sm:text-base leading-relaxed space-y-6">
    {{ page.content }}
  </div>
</section>

{% schema %}
{
  "name": "Statik Sayfa İçeriği",
  "settings": []
}
{% endschema %}
